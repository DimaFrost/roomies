# Supabase backend for realtime sync between flatmates' phones.
# Copy to .env and fill in from your Supabase project (Settings → API).
# Leave unset to run the app in local-only mode (single device, no account).
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_KEY=
